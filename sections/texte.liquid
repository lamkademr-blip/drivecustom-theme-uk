{% schema %}
{
  "name": "CTA Configurateur",
  "tag": "section",
  "class": "section cta-section",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "CAN'T FIND THE CONFIGURATOR FOR YOUR STEERING WHEEL?"
    },
    {
      "type": "richtext",
      "id": "text",
      "label": "Text",
      "default": "<p>Don't worry, we can adapt to your specific needs and create a custom solution for you.</p>"
    },
    {
      "type": "text",
      "id": "button_label",
      "label": "Button text",
      "default": "Get in touch"
    },
    {
      "type": "url",
      "id": "button_link",
      "label": "Button link"
    },
    {
      "type": "header",
      "content": "Background & colours"
    },
    {
      "type": "checkbox",
      "id": "transparent_background",
      "label": "Transparent background",
      "default": false,
      "info": "Enable to make the section background transparent"
    },
    {
      "type": "color",
      "id": "background_color",
      "label": "Background colour",
      "default": "#f5f5f5",
      "info": "Used only when transparent background is disabled"
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "Text colour",
      "default": "#333333"
    },
    {
      "type": "color",
      "id": "button_background",
      "label": "Button background colour",
      "default": "#000000"
    },
    {
      "type": "color",
      "id": "button_text_color",
      "label": "Button text colour",
      "default": "#ffffff"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 20,
      "max": 100,
      "step": 10,
      "unit": "px",
      "label": "Top padding",
      "default": 60
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 20,
      "max": 100,
      "step": 10,
      "unit": "px",
      "label": "Bottom padding",
      "default": 60
    }
  ],
  "presets": [
    {
      "name": "CTA Configurateur",
      "category": "Text"
    }
  ]
}
{% endschema %}

<section class="section cta-configurateur" 
         style="{% unless section.settings.transparent_background %}background-color: {{ section.settings.background_color }};{% else %}background-color: transparent;{% endunless %}
                padding-top: {{ section.settings.padding_top }}px;
                padding-bottom: {{ section.settings.padding_bottom }}px;">
  <div class="page-width">
    <div class="cta-white-rectangle">
      <div class="cta-container">
        {% if section.settings.heading != blank %}
          <h3 class="cta-heading">{{ section.settings.heading }}</h3>
        {% endif %}
        
        {% if section.settings.text != blank %}
          <div class="cta-text">{{ section.settings.text }}</div>
        {% endif %}
        
        {% if section.settings.button_label != blank %}
          <div class="cta-button-wrapper">
            <a href="{{ section.settings.button_link }}" class="cta-button"
               style="background-color: {{ section.settings.button_background }};
                      color: {{ section.settings.button_text_color }};">
              {{ section.settings.button_label }}
            </a>
          </div>
        {% endif %}
      </div>
    </div>
  </div>
</section>

<style>
  .cta-configurateur {
    text-align: center;
  }
  
  .cta-white-rectangle {
    background-color: white;
    border-radius: 20px;
    padding: 40px 20px;
    box-shadow: 0 4px 6px rgba(0,0,0,0.1);
    max-width: 1000px;
    margin: 0 auto;
  }
  
  .cta-container {
    max-width: 800px;
    margin: 0 auto;
  }
  
  .cta-heading {
    font-size: 24px;
    font-weight: 700;
    margin-bottom: 20px;
    text-transform: uppercase;
    line-height: 1.3;
    color: {{ section.settings.text_color }};
  }
  
  .cta-text {
    font-size: 16px;
    line-height: 1.5;
    margin-bottom: 30px;
    color: {{ section.settings.text_color }};
  }
  
  .cta-button-wrapper {
    margin-top: 10px;
  }
  
  .cta-button {
    display: inline-block;
    padding: 12px 30px;
    text-decoration: none;
    font-weight: 600;
    border-radius: 4px;
    transition: opacity 0.2s ease;
  }
  
  .cta-button:hover {
    opacity: 0.9;
  }
  
  @media screen and (max-width: 749px) {
    .cta-white-rectangle {
      border-radius: 15px;
      padding: 30px 15px;
    }
    
    .cta-heading {
      font-size: 20px;
    }
    
    .cta-text {
      font-size: 15px;
    }
    
    .cta-button {
      padding: 10px 25px;
    }
  }
</style>